# 火山引擎TTS环境安装说明

本项目使用火山引擎的语音合成API，需要安装特定的Python和系统依赖。

## 方法1: 使用Conda环境文件（推荐）

这是最简单的方法，会自动安装所有依赖。

```bash
# 创建新环境
conda env create -f environment.yml

# 激活环境
conda activate volcengine_tts

# 运行程序
python tts_gui.py
```

## 方法2: 手动安装

### 1. 创建Conda环境

```bash
conda create -n volcengine_tts python=3.8 -y
conda activate volcengine_tts
```

### 2. 安装Python包

```bash
pip install websockets>=14.0
```

### 3. 安装Qt和多媒体支持

```bash
conda install -c conda-forge pyside2 -y
```

### 4. 安装GStreamer音频支持（关键步骤）

```bash
# 安装GStreamer插件（必须>=1.24版本以支持PulseAudio）
conda install -c conda-forge \
    "gst-plugins-good>=1.24" \
    gst-plugins-base \
    gst-plugins-bad \
    gst-plugins-ugly \
    pulseaudio-client \
    "glib=2.80" \
    "libglib=2.80" -y
```

**重要提示**: 
- GStreamer插件版本必须>=1.24才包含PulseAudio支持
- glib必须使用2.80版本，否则会出现符号未定义错误

## 常见问题

### 问题1: 播放音频时提示"找不到'audio/x-wav'类型可用的解码器"

**原因**: 缺少GStreamer的WAV解码器插件

**解决方案**:
```bash
conda install -c conda-forge gst-plugins-good>=1.24 -y
```

### 问题2: 提示"无法打开音频设备播放"

**原因**: 缺少PulseAudio支持

**解决方案**:
```bash
conda install -c conda-forge \
    "gst-plugins-good>=1.24" \
    pulseaudio-client -y
```

### 问题3: GStreamer警告 "undefined symbol: g_variant_builder_init_static"

**原因**: glib版本过高，与GStreamer不兼容

**解决方案**:
```bash
conda install -c conda-forge "glib=2.80" "libglib=2.80" -y
```

## 验证安装

运行以下命令验证GStreamer是否正确安装：

```bash
# 检查WAV解析器
gst-inspect-1.0 wavparse

# 检查PulseAudio输出
gst-inspect-1.0 pulsesink

# 测试播放WAV文件
gst-launch-1.0 filesrc location=test.wav ! wavparse ! audioconvert ! autoaudiosink
```

如果没有错误，说明环境配置成功！

## 运行程序

```bash
# 确保在正确的conda环境中
conda activate volcengine_tts

# 运行GUI程序
python tts_gui.py
```

## 系统要求

- 操作系统: Linux (Ubuntu/Debian等)
- Python: 3.8+
- Conda/Miniconda
- 系统需要安装PulseAudio音频服务

## 依赖包说明

| 包名 | 版本要求 | 用途 |
|------|----------|------|
| python | 3.8.20 | Python运行环境 |
| websockets | >=14.0 | WebSocket通信 |
| PySide2 | >=5.15.0 | Qt GUI界面 |
| gst-plugins-good | >=1.24 | GStreamer基础插件（含WAV支持） |
| gst-plugins-base | 任意 | GStreamer核心插件 |
| gst-plugins-bad | 任意 | GStreamer扩展插件 |
| gst-plugins-ugly | 任意 | GStreamer编解码器 |
| pulseaudio-client | 任意 | 音频输出支持 |
| glib | =2.80 | GLib库（需指定版本） |
| libglib | =2.80 | GLib库文件（需指定版本） |

## 技术说明

### 为什么必须使用Conda？

PySide2的多媒体功能依赖于系统级的GStreamer库。虽然可以通过apt安装系统包，但：
1. Conda提供了更好的依赖隔离
2. 可以精确控制版本，避免系统库冲突
3. 跨发行版兼容性更好

### 音频播放流程

```
Qt QMediaPlayer → Qt多媒体插件 → GStreamer → PulseAudio → 音频硬件
```

每一层都需要正确配置才能工作。

